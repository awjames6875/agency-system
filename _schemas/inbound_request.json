{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agency-system/inbound_request/v1",
  "title": "Inbound Request",
  "description": "The first payload the orchestrator receives. Normalizes all inbound channels (GHL form, VAPI call, chat, SMS, email, referral) into one shape.",
  "type": "object",
  "required": ["source", "raw_content", "timestamp"],
  "properties": {
    "source": {
      "type": "string",
      "enum": ["ghl_form", "vapi_call", "ghl_chat", "sms", "email", "manual_agent_entry", "referral"],
      "description": "Where the request came from. Orchestrator uses this to weight its classification confidence."
    },
    "raw_content": {
      "type": "string",
      "description": "The full original message, form submission, or call transcript. Never truncated."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "contact": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "phone": { "type": "string" },
        "email": { "type": "string" },
        "ghl_contact_id": { "type": "string" },
        "language": { "type": "string", "enum": ["en", "es"], "default": "en" }
      }
    },
    "vapi_call_summary": {
      "type": "string",
      "description": "Populated only when source=vapi_call. VAPI-generated summary of the call."
    },
    "vapi_call_recording_url": {
      "type": "string",
      "format": "uri",
      "description": "Populated only when source=vapi_call."
    },
    "referral_source": {
      "type": "string",
      "description": "Populated only when source=referral. Name of the referring agent or client."
    }
  }
}
